syntax = "proto3";
package splitsecure.oasis.saml.v2;

import "splitsecure/oasis/saml/v2/entity_descriptor.proto";

// Metadata is the top-level wrapper for SAML metadata.
message Metadata {
  oneof content {
    EntityDescriptor entity_descriptor = 1;
    // EntitiesDescriptor entities_descriptor = 2;
  }
}
